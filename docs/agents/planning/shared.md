# Cross-Level Planning Rules

Stub — content will land in a follow-up commit per
[`docs/plans/agents-md-restructure.md`](/docs/plans/agents-md-restructure.md)
Execution Step 5 (this file lands first; per-level files reference
it).

This file will carry the cross-level planning rules that bind every
plan-drafting session regardless of level (epic / milestone / phase
/ plan-to-pr): the `Verified by:` annotations rule on load-bearing
claims, the falsifiability check on each load-bearing claim, the
rules-vs-estimates labeling discipline, the 5-line code cap in
plans (plan-code-minimalism), the directional-pseudocode framing,
the planning-artifacts-cite-each-other anti-pattern, the
exact-match label quoting rule, the plan-code-reviewer-response
rule (remove the snippet, don't fix the code), and the
Cross-Cutting Invariants section requirement.
