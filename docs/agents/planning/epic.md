# Epic Drafting

Stub — content will land in a follow-up commit per
[`docs/plans/agents-md-restructure.md`](/docs/plans/agents-md-restructure.md)
Execution Step 5.

This file will carry: capability targets vs per-milestone
prescription (epics scope the *what* and *why* of a multi-milestone
arc; they do not prescribe per-phase counts, content, or PR
counts), sizing summaries as estimates pending milestone planning,
per-epic milestone numbering (each epic counts from M1
independently), and the in-place-as-archive pattern (a Landed
epic's `docs/plans/epics/<slug>/` folder stays in place rather
than bulk-moving to `archive/`). Loads
[`shared.md`](./shared.md) for cross-level planning rules.
