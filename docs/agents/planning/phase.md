# Phase Planning Sessions

Stub — content will land in a follow-up commit per
[`docs/plans/agents-md-restructure.md`](/docs/plans/agents-md-restructure.md)
Execution Step 5.

This file will carry: the scoping doc + plan doc artifact split
(Scoping owns / Plan owns), the parallel-drafting-during-prior-
phase-implementation rules with cited-pending-input requirements,
the doc-only-decision-phase carve-out, the
scoping-precedes-plan-drafting check, the plain-language context
preamble requirement, the reality-check gate between scoping and
plan, the URL-retarget assertion-audit rule, the
prefer-existing-wrapper-scripts rule, the spike-before-plan rule
(throwaway `spike/<phase-or-mechanism>` branch on the planning
branch), the PR-count branch test, the `In draft` → `Proposed`
promotion gate, the bans-on-surface-require-rendering rule, and
the cross-app destinations / hard-navigation rule. Loads
[`shared.md`](./shared.md) for cross-level planning rules and
[`plan-to-pr.md`](./plan-to-pr.md) for the Status lifecycle.
